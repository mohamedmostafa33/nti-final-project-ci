[pytest]
DJANGO_SETTINGS_MODULE = reddit_api.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=.
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --verbose
    --tb=short
    --strict-markers
testpaths = .
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
